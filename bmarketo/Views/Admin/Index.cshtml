@{
    ViewData["Title"] = "BackOffice";
}
<form>
<div class="backOffice">
    <div class="container">
        <div class="officeMenu">
            <img src="/Images/logo.svg" class="logo" />
            <h1>Backoffice</h1>
            <hr />
            <button asp-controller="Products"  action="submit" asp-action="RegisterProduct" class="btn-theme">Add/Delete Products</button>
             <button asp-controller="Admin" action="submit" asp-action="OutputUserInfo" class="btn-theme">See/Change Users</button>
            <button class="btn-theme">See Messages</button>
        </div>
    </div>
</div>
</form>